import React from "react";
import { AuthBox, LoginPageHeader } from "../components";

const LoginPage = () => {
  return (
    <AuthBox>
      <LoginPageHeader />
    </AuthBox>
  );
};

export default LoginPage;
